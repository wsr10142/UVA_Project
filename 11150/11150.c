// 題意思考：給定cola初始數量，每三個空瓶可以再換一瓶cola，最多可以喝幾瓶。

#include <stdio.h>
#include <stdlib.h>
#pragma warning(disable : 4996)

// 宣告cola初始數量
int cola = 0;

int ;

int main()
{
    while (scanf("%d %d", &speed, &time) != EOF)
    {
        // 位移為速度乘上時間，題目要求兩倍的時間
        area = speed * (2 * time);

        printf("%d\n", area);
    }

    system("PAUSE");
    return 0;
}