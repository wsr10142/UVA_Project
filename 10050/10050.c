// 題意思考：

#include <stdio.h>
#include <stdlib.h>
#pragma warning(disable : 4996)

int range = 0;
int political = 0;
int hartals = 0;

int main()
{
    scanf("%d", &range);
    for (int i = 0; i < range; i++)
    {
        scanf("%d", &political);
        for (int j = 0; j < political; j++)
        {
            scanf("")
        }
    }

    system("PAUSE");
    return 0;
}