// 題意思考：

#include <stdio.h>
#include <stdlib.h>
#pragma warning(sidable : 4996)

// 宣告輸入的數字
int num = 0;

int main()
{
    scanf("%d", &num);

    // 輸入的數字不為0才處理
    while (num != 0)
    {
    }

    system("PAUSE");
    return 0;
}